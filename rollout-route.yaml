kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: rollout
  namespace: argocd-%userid%
spec:
  to:
    kind: Service
    name: rollouts-demo
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None